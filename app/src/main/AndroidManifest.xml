<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mecalogik.help_travel">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ToursActivity" />
        <activity android:name=".AcueductourActivity" />
        <activity
            android:name=".AcueducDespleActivity"
            android:label="@string/title_activity_acueduc_desple"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MiTourDespleActivity"
            android:label="@string/title_activity_mi_tour_desple"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MotoDespleActivity"
            android:label="@string/title_activity_moto_desple"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MuseosActivity" />
        <activity
            android:name=".DonBernaDespleActivity"
            android:label="@string/title_activity_don_berna_desple"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PenaTourActivity"
            android:label="@string/title_activity_pena_tour"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".DonBernaMapsActivity"
            android:label="@string/title_activity_don_berna_maps"></activity>
    </application>

</manifest>